// 先尝试 暴力 再优化
/**
 * 
 * @param {number[]} piles 
 * @param {number} H
 * @param {number}
 */
var minEatingSpeed = function(piles, H) {
    let lo = 1, hi = Math.max(...piles);
    console.log(lo, hi);
    // 求出最大香蕉树，在某把中
    

};